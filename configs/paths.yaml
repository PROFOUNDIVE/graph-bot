libs:
  bot_root: "libs/buffer-of-thought-llm"
  got_root: "libs/graph-of-thoughts"
data:
  raw: "data/raw"
  interim: "data/interim"
  processed: "data/processed"
outputs:
  runs: "outputs/runs"
  artifacts: "outputs/artifacts"
